
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ОбменДанными.Загрузка = Истина Тогда 
		Возврат;
	КонецЕсли;
	
	Если РежимЗаписи=РежимЗаписиДокумента.Запись И НЕ ПометкаУдаления Тогда	
		РежимЗаписи = РежимЗаписиДокумента.Проведение;
	КонецЕсли;	
	
	ДопЗатратыНаМатериалы 	= Документы.ЖБИ_РасчетПлановыхСтавок.РасчитатьСреднююСтавкуПоДопРасходам(ДопЗатраты);
	МенеджерЛогист 			= Пользователи.ТекущийПользователь();
	
КонецПроцедуры

