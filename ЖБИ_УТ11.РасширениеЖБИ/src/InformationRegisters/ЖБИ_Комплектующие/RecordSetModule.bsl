
Процедура ПриЗаписи(Отказ, Замещение)
	
	Для Каждого НомСтр Из ЭтотОбъект Цикл
		Если НомСтр.Номенклатура = НомСтр.Комплект Тогда 
			Отказ = Истина;
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Нельзя указывать одинаковую Номенкатуру и Комплект!");
			Прервать;
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры
