
Функция GetdataExecute(Запрос)
	
	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-type", "application/JSON; charset=utf-8");
	
	СообщениеJson = Запрос.ПолучитьТелоКакСтроку("UTF-8");		
	ТекстОтвета = ЖБИ_ОбменСайтом.ExecuteTask(СообщениеJson);			
	Ответ.УстановитьТелоИзСтроки(ТекстОтвета,КодировкаТекста.UTF8,ИспользованиеByteOrderMark.НеИспользовать);
	
	Возврат Ответ;
	
КонецФункции

